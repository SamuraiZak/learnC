#include <stdio.h>

int main()
{
    // char name[] = "zaki";
    // int age = 24;
    // float gpa = 3.33;
    // char singleCharacter = 'Z';

    // printf("%s (%c) is %d years old and graduated with cgpa %0.2f", name, singleCharacter, age, gpa);

    return 0;
}